// const draw = (dimension : {a: number, b:number}) => {

// }

// draw({
//     a: 1,
//     b: 2
// })

interface Dimension{
    x: number;
    y: number;
}

const draw = (dimension: Dimension) => {

}

draw({
    x: 10,
    y: 20
})

class Draw{
    x: number;
    y: number;
}